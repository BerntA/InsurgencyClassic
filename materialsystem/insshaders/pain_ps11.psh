ps.1.1

; Pain Shader - Shader Model 1.1
;
; Inputs:
;   c0 - Base image weight
;   c1 - Blur image weight
;   c2 - Haze "color" (anticolor)

tex t0
tex t1
tex t2
tex t3

mad r0, t0.rgb, c0 c2
mad r0, t1.rgb, c1, r0
mad r0, t2.rgb, c1, r0
mad r0, t3.rgb, c1, r0